#######################
# Update configs repo #
#######################

# brew
# leaves
0 */2 * * * cd ~/Desktop/configs/brew && brew leaves > leaves.txt
# casks
1 */2 * * * cd ~/Desktop/configs/brew && brew list --cask > list--cask.txt
# list
2 */2 * * * cd ~/Desktop/configs/brew && brew list > list.txt

# crontab
3 */2 * * * cd ~/Desktop/configs/crontab && crontab -l > user

# push configs to git
30 */2 * * * cd ~/Desktop/configs && git -c user.name="cronjob" commit -a -S -m "refresh: auto sync all tracked configs" && git push

###########################
# Refresh direnv database #
###########################
@reboot direnv prune

###############
# Empty trash #
###############
@reboot osascript -e 'tell application "Finder" to empty trash'

########
# brew #
########
@reboot brew autoremove
@reboot brew cleanup

